{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "nvm:use": "bash -lc 'unset npm_config_prefix; export NVM_DIR=\"$HOME/.nvm\"; if [ -s \"$NVM_DIR/nvm.sh\" ] && [ -f .nvmrc ]; then . \"$NVM_DIR/nvm.sh\"; NODE_VERSION=\"$(cat .nvmrc)\"; nvm install \"$NODE_VERSION\" >/dev/null && nvm use \"$NODE_VERSION\" >/dev/null; fi'",
    "predev": "npm run nvm:use",
    "prestart": "npm run nvm:use",
    "dev": "nodemon src/server.js",
    "start": "node src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "mysql2": "^3.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
